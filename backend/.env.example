ENVIRONMENT=production
DEBUG_MODE=false

# Gere com: openssl rand -hex 32
SECRET_KEY=COLOQUE_AQUI_UM_HEX_DE_64

# CORS
CORS_ORIGINS=https://tr4ction-v2-agent.vercel.app,https://api.tr4ction.ai

# Banco (SQLite temporário; pronto para migrar p/ Postgres futuramente)
DATABASE_URL=sqlite:///./data/tr4ction.db

# JWT (produção: OBRIGATÓRIO definir JWT_SECRET_KEY)
JWT_SECRET_KEY=
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# Chroma (via serviço interno Docker)
CHROMA_HOST=chroma
CHROMA_PORT=8000
CHROMA_DB_DIR=/app/data/chroma_db

# Provedores de IA (opcionais)
# GROQ_API_KEY=
# GROQ_MODEL=llama-3.3-70b-versatile
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4.1-mini

# Diretórios de dados (já dentro de /app/data no container)
DATA_DIR=/app/data
TEMPLATES_IMAGES_DIR=/app/data/templates_images
# ======================================================
# [CONFIG] TR4CTION BACKEND - EXEMPLO DE CONFIGURAÇÃO
# ======================================================
# Copie este arquivo para .env e configure os valores
# Execute: python validate_env.py para validar

# ======================================================
# [LLM] PROVIDER - ESCOLHA: groq, openai, ou offline
# ======================================================
# GROQ (Recomendado - grátis e rápido)
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GROQ_MODEL=llama-3.3-70b-versatile

# OPENAI (Alternativa - pago)
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# OPENAI_MODEL=gpt-4-mini

# Se nenhuma chave for fornecida, o sistema rodará em MODO OFFLINE (mock)

# ======================================================
# [EMBEDDINGS] ESCOLHA O PROVIDER
# ======================================================
# "huggingface" = API externa (RECOMENDADO para EC2 t3.micro com RAM limitada)
# "local" = sentence-transformers local (melhor performance, requer 2GB+ RAM)
EMBEDDING_PROVIDER=huggingface

# Token do Hugging Face (obtenha em: https://huggingface.co/settings/tokens)
HF_API_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Modelo de embeddings (384 dimensões - otimizado para performance)
HF_EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

# Diretório local de ChromaDB (consolidado)
CHROMA_DB_DIR=./data/chroma_db

# ======================================================
# [DATABASE] CONFIGURAÇÃO
# ======================================================
DATABASE_URL=sqlite:///./tr4ction.db

# ======================================================
# [SECURITY] MUDE ESTAS VARIÁVEIS EM PRODUÇÃO!
# ======================================================

# JWT Secret Key - CRITICAL FOR PRODUCTION SECURITY
# Generate a strong secret with: openssl rand -hex 32
# ⚠️ NEVER use the default value in production!
# Default: Stable dev secret (safe for development only)
JWT_SECRET_KEY=tr4ction-change-this-in-production-openssl-rand-hex-32
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# CORS - Origens permitidas (separadas por vírgula)
# Desenvolvimento: http://localhost:3000
# Produção: https://seudominio.com, https://www.seudominio.com
CORS_ORIGINS=https://tr4ction-v2-agent.vercel.app,https://www.tr4ction-v2-agent.vercel.app,http://localhost:3000,http://127.0.0.1:3000

# ======================================================
# [LIMITS] LIMITES DE UPLOAD E RATE LIMITING
# ======================================================
MAX_UPLOAD_SIZE_MB=50
ALLOWED_EXTENSIONS=.pdf,.pptx,.docx,.txt,.xlsx

RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_SECONDS=60

# ======================================================
# [ENVIRONMENT] AMBIENTE
# ======================================================
# "development" ou "production"
ENVIRONMENT=development
DEBUG_MODE=false

# ======================================================
# [OPTIONAL] CONFIGURAÇÕES ADICIONAIS
# ======================================================
# Modo offline forçado (para testes sem API)
# FORCE_OFFLINE=false

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Production recommendation: INFO or WARNING
LOG_LEVEL=INFO

# ======================================================
# 🚦 RATE LIMITING
# ======================================================
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_SECONDS=60

# ======================================================
# 🔧 AMBIENTE
# ======================================================
ENVIRONMENT=development
DEBUG=false
