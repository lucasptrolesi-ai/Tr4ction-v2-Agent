# ======================================================
# 🔧 TR4CTION BACKEND - EXEMPLO DE CONFIGURAÇÃO
# ======================================================
# Copie este arquivo para .env e configure os valores

# ======================================================
# 🤖 LLM PROVIDER (Groq)
# ======================================================
GROQ_API_KEY=sua-chave-groq-aqui
GROQ_MODEL=llama-3.3-70b-versatile

# ======================================================
# 💾 BANCO DE DADOS
# ======================================================
CHROMA_DB_DIR=./chroma_db
DATABASE_URL=sqlite:///./tr4ction.db

# ======================================================
# 🧠 EMBEDDINGS - ESCOLHA O PROVIDER
# ======================================================
# "huggingface" = API externa (RECOMENDADO para EC2 t3.micro)
# "local" = sentence-transformers local (requer mais RAM)
EMBEDDING_PROVIDER=huggingface

# Token do Hugging Face (obtenha em: https://huggingface.co/settings/tokens)
HF_API_TOKEN=hf_seu_token_aqui

# Modelo de embeddings (384 dimensões)
# Recomendado: BAAI/bge-small-en-v1.5 (funciona com a API atual)
HF_EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

# ======================================================
# 🔐 SEGURANÇA - IMPORTANTE: MUDE EM PRODUÇÃO!
# ======================================================
# ======================================================
# Gere com: openssl rand -hex 32
JWT_SECRET_KEY=GERE-UMA-CHAVE-SEGURA-COM-OPENSSL
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# CORS - Origens permitidas (separadas por vírgula)
# Em produção: https://seudominio.com
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ======================================================
# 📁 LIMITES DE UPLOAD
# ======================================================
MAX_UPLOAD_SIZE_MB=50
ALLOWED_EXTENSIONS=.pdf,.pptx,.docx,.txt,.xlsx

# ======================================================
# 🚦 RATE LIMITING
# ======================================================
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_SECONDS=60

# ======================================================
# 🔧 AMBIENTE
# ======================================================
ENVIRONMENT=development
DEBUG=false
